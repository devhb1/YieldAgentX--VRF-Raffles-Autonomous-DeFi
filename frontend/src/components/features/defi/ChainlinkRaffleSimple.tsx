/** * Chainlink Raffle Simple Component * Simplified wrapper that redirects to the working raffle page */'use client';import React from 'react';import { Card, CardContent, CardDescription, CardHeader, CardTitle } from '@/components/ui/card';import { Button } from '@/components/ui/button';import { useRouter } from 'next/navigation';import { Ticket, Trophy, ExternalLink } from 'lucide-react';export default function ChainlinkRaffleSimple() {  const router = useRouter();  const goToRaffle = () => {    router.push('/raffle');  };  return (    <div className="space-y-6">      <Card>        <CardHeader>          <CardTitle className="flex items-center gap-2">            <Trophy className="h-5 w-5" />            Chainlink Raffle          </CardTitle>          <CardDescription>            Join our decentralized raffle for a chance to win ETH          </CardDescription>        </CardHeader>        <CardContent className="space-y-4">          <div className="text-center py-6">            <Ticket className="h-12 w-12 mx-auto mb-4 text-blue-500" />            <h3 className="text-lg font-semibold mb-2">Win ETH!</h3>            <p className="text-muted-foreground mb-4">              Provably fair raffle powered by Chainlink VRF            </p>            <Button onClick={goToRaffle} className="inline-flex items-center gap-2">              <ExternalLink className="h-4 w-4" />              Play Now            </Button>          </div>        </CardContent>      </Card>    </div>  );}